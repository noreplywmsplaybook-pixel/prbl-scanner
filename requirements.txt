# prbl-scanner runtime dependencies
# The scanner uses Python stdlib (urllib, re, pathlib, dataclasses) for all
# static analysis and package registry lookups — no external HTTP library needed.
# The packages below are required only if you use the optional CLI or API server.

fastapi>=0.137.1
uvicorn>=0.22.0
requests>=2.33.0
urllib3>=2.7.0
starlette>=1.0.1
